import { FormatTimeRangePipe } from './format-time-range.pipe';

describe('FormatTimeRangePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTimeRangePipe();
    expect(pipe).toBeTruthy();
  });
});
